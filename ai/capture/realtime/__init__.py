"""디스코드 실시간 전사 경로. 폐기했다.

회의 중 전사를 접고 종료 뒤 배치(stt/batch.py)로 가면서 이 폴더의 Cog 와 게시기는 프로세스에 올리지
않는다. sink, VAD, 말 필터, 트랙 기록은 capture/ 와 stt/ 본체로 옮겨 배치가 그대로 쓴다. 남긴 이유는
실측 근거(decision_log 0003~0005)가 이 코드를 가리키기 때문이고, 팀이 정하면 폴더째 지운다.
"""
