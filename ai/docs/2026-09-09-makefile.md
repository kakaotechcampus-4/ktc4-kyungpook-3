# 봇 실행 파이프라인 Makefile 추가

- 날짜: 2026-09-09
- 이슈: #9
- PR: #10
- 브랜치: chore/9-makefile
- 작성자: 유재환

## 한 일

가상환경 생성·의존성 설치·봇 실행·테스트를 Makefile 명령 하나씩으로 정리
(`make setup` / `make run` / `make test` / `make clean`).

## 왜

이슈 #9. 팀원마다 `uv venv`/`uv pip install` 순서를 매번 다시 찾아보게 하지 않으려고 단축 명령으로 묶었다.

## 결과

`make test`로 pytest 10개 통과 확인.

## 다음

STT(faster-whisper) 전사 구현 → PR #12.
