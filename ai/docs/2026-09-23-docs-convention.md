# 진행 로그(docs/) 디렉토리 컨벤션 추가

- 날짜: 2026-09-23
- PR: #67
- 브랜치: docs/progress-log-convention
- 작성자: 유재환

## 한 일

push 단위로 한 일을 남기는 `docs/` 디렉토리와 템플릿(`docs/README.md`)을 신설.
`decision_log/`와 역할이 겹치지 않도록 표로 구분(무엇을 했는지 vs 트레이드오프 근거).

이 파일을 포함해 이전에 merge된 AI 파트 PR 10개 + 이번 세션에 push한 PR 3개를 이 템플릿으로
소급 작성했다.

## 왜

push마다 뭘 했는지 기록을 남기고 싶다는 요청 — PR 본문은 머지되고 나면 다시 찾아보기 번거롭고,
저장소 안에서 시간순으로 훑어볼 수 있는 별도 로그가 있으면 좋겠다고 판단.

## 결과

`docs/README.md` + 소급 작성 13개 파일.

## 다음

앞으로 push할 때마다 이 템플릿으로 하나씩 추가.
