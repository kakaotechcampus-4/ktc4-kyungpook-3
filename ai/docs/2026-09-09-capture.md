# 화자별 녹음 캡처 구현

- 날짜: 2026-09-09
- 이슈: #1
- PR: #6
- 브랜치: feature/1-capture
- 작성자: 유재환

## 한 일

- 음성채널 입장/녹음 시작/종료/퇴장 커맨드(`/join /record /stop /leave`)
- 화자별로 분리된 오디오 파일 저장 (`recordings/{user_id}_{ts}.wav`)
- 세션 단위로 참가자·파일·길이 정보를 담은 메타데이터 생성 (`recordings/session_{ts}.json`)
- 별도 봇 프로세스 없이도 캡처 기능만 독립적으로 검증 가능한 실행기 (`capture/run_recorder.py`)

## 왜

이슈 #1(화자별 녹음 캡처). 파이프라인 전체의 입력이 되는 첫 단계라 여기서부터 시작했다.

`discord_adapter.py`가 봇 프로세스를 직접 띄우지 않고 `discord.Cog`(`RecordingCog`)만 export
하는 구조로 만들었다 — BE가 실제 봇(`backend/bot/main.py`)을 소유하고 이 Cog를 갖다 붙이는
식으로 캡처 로직과 봇 인프라를 분리하기 위한 선택.

## 결과

실제 5~6인 음성채널 녹음으로 로컬 검증 완료. `pytest` 통과 확인. 이슈 #1 체크리스트 4개 항목 전부 반영.

## 다음

README/가이드 문서화 → PR #7. STT(전사) 구현 → PR #12.
