# faster-whisper 전사 구현

- 날짜: 2026-09-09
- 이슈: #11
- PR: #12
- 브랜치: feature/11-stt
- 작성자: 유재환

## 한 일

- 녹음된 화자별 wav를 faster-whisper로 텍스트로 변환하는 기능 구현
- 모델별 비교 지원 (small/medium/large-v3/large-v3-turbo)
- 세션 단위로 화자별 결과를 시간순으로 합쳐서 하나의 전사 파일로 생성
- WER/CER 계산 + 크로스토크 체크리스트로 정확도 리포트 자동 생성 (`eval_report.md`)
- 모델별 처리 시간 누적 기록 (`timing_summary.md`)

## 왜

이슈 #11. 캡처된 오디오를 텍스트로 바꿔야 이후 추출/판단 단계로 넘어갈 수 있어서, 파이프라인의
두 번째 단계로 진행했다.

## 결과

**코드 이식 검증** (다른 데이터로 확인 — 이 저장소의 실제 6인 녹음 아님)

| 항목 | 값 | 기준 |
|---|---|---|
| 단일 화자 WER(정규화, small) | 6.5% | 15% 이하 |
| 처리 시간(1분 오디오당, small) | 8.6초 | 30초 이내 |
| 처리 시간(1분 오디오당, medium) | 23.6초 | 30초 이내 |

`tests/test_transcribe.py` 5개 케이스(wav stem 파싱, 세션 병합, 처리시간 집계 등) — faster-whisper
없이 스텁 모델로 실행. 전체 스위트 10개 통과.

## 다음

- 이 저장소의 실제 5~6인 `meeting5_*` 녹음 → 전사 → WER/크로스토크 평가 (후속 작업으로 남김)
- STT 검증 방법론 문서화 → PR #14
