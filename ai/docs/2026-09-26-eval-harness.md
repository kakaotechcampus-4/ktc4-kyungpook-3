# 전사 단위와 전사·수신 상수의 근거를 재는 평가 하네스와 2026-09-26 측정

- 날짜: 2026-09-26
- 이슈: #89
- PR: #미정
- 브랜치: feature/89-eval-harness
- 작성자: 김동우

## 한 일

- 상수 목록 `stt/eval/constants.py`. 전사·수신 경로의 이름 붙은 숫자 상수 44개를 경로, 쓰이는 자리, 근거 종류(여기서 측정 / 다른 측정 결과 파일 / 외부 사양 / 설계 판단 / 여기서 측정 불가), 흔들 값, 주 지표, 다시 잴 조건으로 적었다. 목록에 빠진 상수가 있으면 테스트가 깨진다. `overrides()` 는 모듈 속성과 정의 시점에 묶인 기본 인자(`__defaults__`, `__kwdefaults__`)를 같이 바꾸고 되돌린다 (ca7eed5)
- 지표 `stt/eval/textmetrics.py`. 절대 오류 글자, 문장 끝 보존(클립 이음 자리 따로), 클립 가장자리 오류, 환각 구절, 용어 재현, 턴 경계를 넘어간 글자 (b874330, a67e106, 8b48117)
- 캐시 `stt/eval/sttcache.py`. 오디오 내용 해시로 같은 조각을 다시 전사하지 않고, 호출마다 길이·시간·오류를 남긴다 (119f9e7)
- `stt/eval/golden.py` 의 채점을 `session_metrics` 로 떼어 민감도 측정이 그대로 쓴다 (ab2b563)
- 민감도 측정 `stt/eval/sensitivity.py` 와 표 `stt/eval/sensitivity_report.py`. 골든 폴더의 모든 회의에 기본값, 결정성 확인, 디더 잡음, 단위 모드, 상수 하나씩 흔들기, 용어 힌트를 돌리고 평탄·경사·절벽·발동 안 함으로 판정한다. `rescore` 는 저장된 줄로 지표를 다시 센다. `align-sweep` 은 정렬본을 만드는 상수를 바꿔 다시 정렬한다 (aac9b78, 057520b, 8056158 등)
- 재배치 합성 회의 `stt/eval/scenarios.py`. 정렬본 조각을 다시 놓아 교대 직후, 끼어들기, 짧은 대답, 긴 침묵 뒤 첫 발화, 할일과 마감이 다른 줄, 30초 넘는 독백을 만든다 (db36db2, 274690c)
- 추출 비교 `stt/eval/extract_diff.py`. 지민의 `extract_tasks` 를 호출만 한다. 정답 전사 추출 세 번의 합의 대비 놓친 할일·더한 할일·담당자·마감 변화, 합성 회의에 적은 의도 대비 담당자·마감 보존을 센다 (8fea001, 320d8cc, 604ad28)
- 골든 폴더 형식과 재측정 절차 `stt/eval/README.md`, 결정 기록 `decision_log/0012-*.md`, 결과 `stt/eval/results/2026-09-26-units/`

## 왜

단위(클립·턴·묶음)와 그 상수들은 0008 에서 한 값으로만 쟀고, 멘토 리뷰(#45 1차 본문 5, 고민 5)가 턴 기준을 화자 교대·끼어들기·마감 분리 발화에서 추출 누락으로 비교하라고 했다. 결정과 트레이드오프는 `decision_log/0012` 에 적고 여기서는 다시 쓰지 않는다.

상수 값을 바꿔 끼우는 방법을 비교했다. 운영 코드를 호출 시점에 전역을 읽게 고치는 것은 `run()` 을 PR #83 이 고치는 중이라 버렸다. 함수를 감싼 대리 함수로 바꿔치기하는 것은 안에서 위치 인자로 넘기는 호출과 인자가 겹쳐 버렸다. 함수 객체의 기본 인자를 직접 바꾸고 되돌리는 쪽을 골랐다.

LLM 호출은 openai SDK 가 공유 venv 에 없어(requirements.txt 에는 있다) requests 로 같은 json_schema 요청을 보냈다. `.env` 에 LLM_API_KEY 가 없어 ELICE_API_KEY 로 엘리스 게이트웨이의 gemini-2.5-flash-lite 를 불렀다.

## 결과

결정과 수치는 `decision_log/0012-transcription-units-and-constants.md`, 표는 `stt/eval/results/2026-09-26-units/tables/`. 요약만 적는다.

| 항목 | 값 |
|---|---|
| 기본값 | 전부 유지. 바꾼 상수 값 없음 |
| 로컬 turbo 잡음 폭 | 정렬본 6자, 재배치 합성 8자 (디더 10회 + 기본값) |
| 단위 (정렬본 오류 글자) | chunk 80, 턴마다 묶음 80, 클립 하나씩 95 |
| 턴 공백과 추출 | 마감이 1.5초 쉼 뒤에 따로 온 경우 TURN_GAP_S 0.5·1초는 마감 4개 중 1개, 2~8초는 3개를 붙였다 |
| 짧은 대답 | 합성에서 말 필터가 45발화 중 7개를 거름. 필터를 끄면 1개. 정렬본에서는 거른 것 0 |
| Elice | 2026-09-26 02:04~08:24 전사 API 가 응답하지 않아 아직 안 잼 |
| 비용 | 154.8원. 추출 LLM 280회 145.6원(입력 434,392·출력 151,323 토큰, Google 공시 단가와 1,400원/$ 가정), Elice 전사 확인 호출 24건 9.3원(시간 초과도 과금된 것으로 셈) |

재현: `python -m stt.eval.sensitivity run --golden-root <골든> --golden-root <합성> --out stt/eval/results/2026-09-26-units --cache <캐시> --noise-seeds 10` 다음 `report`. 테스트는 500 통과(새 테스트 76).

## 남은 것

- Elice 로 단위 모드와 CHUNK_GAP_S·CHUNK_MAX_S·TAIL_PAD_S 흔들기, 같은 설정 세 번으로 잡음 폭 재기. 명령은 `--backend elice --yes`, 예상 1,500원 이내
- 실녹음 골든셋이 들어오면 같은 명령으로 다시 재기. 짧은 대답과 말 필터를 먼저 본다
- 수신 상수(재정렬 창, 패킷 공백 등)는 실서버 패킷 도착 기록이 있어야 잰다

## 생각해볼 점

- 추출은 줄을 넘어 마감을 잇지 않는다. 할일과 마감이 다른 화자의 말로 갈리면 턴 상수로는 못 푼다. 추출(지민)과 정할 일이다
- 말 필터는 배치 클립(꼬리 0.1초)에서 짧은 대답을 거른다. 필터가 보는 클립 모양을 바꾸는 것은 코드 변경이라 이 작업에서 하지 않았다
- 공유 venv 에 openai 가 없다(requirements.txt 에는 있다). `.env` 에 LLM_API_KEY 가 없어 이 측정은 ELICE_API_KEY 로 엘리스 게이트웨이의 gemini-2.5-flash-lite 를 불렀다. 운영 봇의 추출 설정을 무엇으로 할지 정해야 한다
- 정렬본 두 회의는 유재환 첫 발화 끝이 마지막 발화 자리에 있다. 화자별 CER 에는 영향이 없고 턴 단위 지표와 추출 비교에 늘 섞인다
