# ── Phase 0: 캡처(Discord) + STT ─────────────────────────────────────────────
# Discord 음성 "수신"(녹음)은 2026-03-01 부터 DAVE(E2EE) 필수.
#   py-cord 2.8.x 는 DAVE 송신만 지원 → 녹음 불가. PR #3159(2.9.0rc1 예정)만 수신 가능.
#   2.9.0rc1 이 PyPI 에 올라오면  py-cord[voice]>=2.9.0rc1  로 바꾸세요.
py-cord[voice] @ git+https://github.com/Pycord-Development/pycord@refs/pull/3159/head
faster-whisper>=1.0
jiwer>=3.0            # WER/CER (ai/stt/eval)
soundfile>=0.12       # WAV 파일 I/O (capture/audio.py)
requests>=2.31        # HTTP API (stt/elice.py)

# ── Phase 1~2: LLM (Google Gemini 무료 티어) ─────────────────────────────────
google-genai>=1.0

# ── 개발 ─────────────────────────────────────────────────────────────────────
pytest>=8.0
