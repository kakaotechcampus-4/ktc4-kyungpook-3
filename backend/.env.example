# MVP: SQLite / 확장: PostgreSQL
DATABASE_URL=sqlite:///./mm.db
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/mm

# Notion 반영 워커 주기(초). 0이면 워커를 띄우지 않는다.
NOTION_SYNC_INTERVAL_SECONDS=5
