# 프론트엔드 개발 시 고민

화면·컴포넌트·스타일을 만들 때 헷갈리거나, 두 길이 있어 고른 지점만 적는다.

`docs/decision/`은 제품 범위·라우팅처럼 개발 전에 합의한 결정이다.
여기에는 “이 버튼을 어떻게 짜지?”, “클래스를 어떻게 붙이지?”, “이 스펙 두 줄이 충돌하는데?”처럼 **코드를 짜는 사람의 고민**만 둔다.

둘 다 결정 기록이고, 다른 것은 **층위와 시점**이다.

| | `docs/decision/` | `docs/impl-decision/` |
|---|---|---|
| 언제 | 개발 전에 합의 | 코드를 짜다 그 자리에서 |
| 무엇 | 제품 범위·라우팅·스택 | 컴포넌트·클래스·토큰 |
| 형태 | 한 파일에 `D-번호` 누적 | 주제당 한 파일 |

넣지 않는 것:

- 빌드·CI가 통과했다는 기록
- 패키지 버전이 스펙 문법을 받았다는 확인
- 로컬 OS/줄바꿈/툴체인 이슈
- 스펙에 이미 채택으로 적힌 값의 재서술

## 파일 형식

이름은 `YYYY-MM-DD-영문-슬러그.md`. 날짜는 고민한 날이다.

```markdown
# 제목 — 무엇이 갈렸는지 한 줄로

- 날짜: 2026-09-16
- 상태: 결정 | 확인 필요

## 고민
무엇이 헷갈렸는지, 길이 왜 갈렸는지.

## 고른 길
고른 것. 코드가 있으면 붙인다.

## 왜
근거. 다른 길을 왜 안 골랐는지까지.

## 다시 고민할 때
이 결정을 다시 볼 조건. 없으면 생략한다.
```

`다시 고민할 때`가 이 폴더의 핵심이다. 여기 적힌 문서는 닫힌 기록이 아니라 **재검토 조건을 달고 살아 있는 결정**이다.
`상태: 확인 필요`는 아직 안 닫혔다는 뜻이고, 그 조건이 오면 문서를 고친다.

## 코드에서 가리키기

결정이 코드 모양을 바꿨으면 그 자리에 경로를 남긴다. 나중에 읽는 사람이 “왜 이렇게 짰지”를 여기서 찾는다.

```ts
/* 근거: m2-design-tokens.md §7-1, docs/impl-decision/2026-09-16-no-cn-clsx-tailwind-merge.md */
```
